# Screenshots

This directory is reserved for public product screenshots and short demos.

Giit is currently in private development, so the README uses a lightweight
interface preview instead of a release screenshot. Replace that preview with
actual product screenshots when the public build is ready to show.
